import PagePlaceholder from './PagePlaceholder';

const HealthPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Health"
      description="Health dashboard features will be added in subsequent iterations."
    />
  );
};

export default HealthPage;
