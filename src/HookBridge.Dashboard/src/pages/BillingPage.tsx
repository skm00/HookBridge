import PagePlaceholder from './PagePlaceholder';

const BillingPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Billing"
      description="Billing dashboard features will be added in subsequent iterations."
    />
  );
};

export default BillingPage;
