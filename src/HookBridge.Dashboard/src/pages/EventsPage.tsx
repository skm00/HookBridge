import PagePlaceholder from './PagePlaceholder';

const EventsPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Events"
      description="Events dashboard features will be added in subsequent iterations."
    />
  );
};

export default EventsPage;
