import PagePlaceholder from './PagePlaceholder';

const ApiKeysPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="API Keys"
      description="ApiKeys dashboard features will be added in subsequent iterations."
    />
  );
};

export default ApiKeysPage;
