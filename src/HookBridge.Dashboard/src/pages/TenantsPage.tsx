import PagePlaceholder from './PagePlaceholder';

const TenantsPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Tenants"
      description="Tenants dashboard features will be added in subsequent iterations."
    />
  );
};

export default TenantsPage;
