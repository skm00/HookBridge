import PagePlaceholder from './PagePlaceholder';

const SubscriptionsPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Subscriptions"
      description="Subscriptions dashboard features will be added in subsequent iterations."
    />
  );
};

export default SubscriptionsPage;
