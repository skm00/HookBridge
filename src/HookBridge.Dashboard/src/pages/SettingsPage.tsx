import PagePlaceholder from './PagePlaceholder';

const SettingsPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Settings"
      description="Settings dashboard features will be added in subsequent iterations."
    />
  );
};

export default SettingsPage;
