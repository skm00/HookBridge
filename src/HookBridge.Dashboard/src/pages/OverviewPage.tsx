import PagePlaceholder from './PagePlaceholder';

const OverviewPage = (): JSX.Element => {
  return (
    <PagePlaceholder
      title="Overview"
      description="Overview dashboard features will be added in subsequent iterations."
    />
  );
};

export default OverviewPage;
