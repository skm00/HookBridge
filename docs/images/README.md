# Image Placeholders

This directory is reserved for README images.

Expected files when final assets are available:

- `architecture.png` — architecture diagram used by the README architecture section.
- `dashboard.png` — dashboard screenshot used by the README screenshots section.

Binary image assets are intentionally not included in this documentation-only change so pull request tools that reject binary files can render the diff normally.
